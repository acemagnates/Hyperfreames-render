# VISUAL STORYBOARD — Obsidian30

## Logline

**Light fractures through volcanic black glass** — a wordless cinematic arc from void → crack → refraction → orbit → ignition → stillness. Premium documentary lane: physical shadows, film grain, continuous camera motion, solid `#0a0604` void.

**Emotional arc:** Dread → Tension → Wonder → Hypnosis → Triumph → Peace.

**Palette:** void `#0a0604`, amber `#ff9a3c`, gold `#ffd166`, ember `#c41e3a`, ash `#2a1f18`.

---

## Clip 01 — ABYSS (0–5s)

- Floating ember particles (deterministic LCG), slow camera drift `translateY`.
- Hero: `OBSIDIAN` in Cormorant Garamond, gold rim light via layered text-shadow.
- Fine vignette + grain overlay (canvas, no full-frame blur).

## Clip 02 — FISSURE (5–10s)

- SVG crack paths stroke-dashoffset reveal; gold bleed along cracks.
- Label `FISSURE` monospace HUD.

## Clip 03 — PRISM (10–16s)

- CSS 3D stage `perspective: 1200px`; three glass panels `rotateY` stagger.
- Light shafts: narrow divs with `linear-gradient`, `mix-blend-mode: screen`.

## Clip 04 — ORBIT (16–22s)

- Golden torus-ring (border + box-shadow stacks), camera `rotateZ` + `scale`.
- `ORBIT` kinetic type.

## Clip 05 — IGNITE (22–27s)

- Concentric shock rings expand; core flare `scale` + opacity pulse (single cycle, no `repeat: -1`).

## Clip 06 — LOCK (27–30s)

- Mirror reflection split; `LIGHT / FRACTURED` tag; fade to black.
