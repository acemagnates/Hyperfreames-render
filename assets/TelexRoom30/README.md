# TelexRoom30 assets

- Optional: `narration.mp3` — used by `scripts/assemble-telexroom30-downloaded.sh` when present.
- `sfx/` — populated by the assemble script (procedural stems) unless you supply your own WAVs with the same filenames referenced in that script.
