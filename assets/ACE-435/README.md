# ACE-435 assets

## Narration

`narration.mp3` is currently a **58s silent placeholder** so `npm run check` succeeds when this project is wired as `index.html`. Replace it with your final VO (include **≥3s silence** after the last spoken syllable, wall ~55s, through ~58s total).

## SFX stems (FFmpeg assembly)

Per Master Timeline / manifest:

| Wall (s) | Stem | Notes |
|---------:|------|--------|
| 0.00 | `sfx/cinematic_drone.wav` | Full bed; outro decay per spec |
| 10.21 | `sfx/sub_bass_impact.wav` | WEAPONIZE |
| 22.70 | `sfx/whoosh.wav` | LOGIC / blueprint reveal |
| 30.92 | `sfx/glass_shatter.wav` | COPYRIGHTED |
| 43.00 | *(mix automation)* | SILENCE_DROP — duck VO + drone |
| 53.48 | `sfx/slam.wav` | CRIME |

Place files under `assets/ACE-435/sfx/` (paths match manifest `stem_file`).

**Current stems:** procedural placeholders generated by `scripts/assemble-ace435-downloaded.sh` (replace with licensed/Freesound masters when shipping). Manifest: `assets/ACE-435/sfx-manifest.json`.

## Assembled outputs (local)

- **CI clips (downloaded):** `renders/out/ACE-435/ci-artifacts/<clip-id>/` (legacy), **`ci-artifacts-draft/`** (run `25986265649`, draft), **`ci-artifacts-standard/`** (run `25986879501`, standard).
- **Concat (silent):** `renders/out/ACE-435/ACE-435-{draft|standard}-concat-nosound.mp4`
- **Final VO + SFX (A/B):**  
  - **`renders/out/ACE-435/ACE-435-standard-with-audio-sfx.mp4`** — standard CI renders + same mix  
  - **`renders/out/ACE-435/ACE-435-draft-with-audio-sfx.mp4`** — draft CI renders + same mix  
  Rebuild either with:
  `bash scripts/assemble-ace435-downloaded.sh ./test_12.mp3 ./renders/out/ACE-435/ci-artifacts-standard standard`  
  (`HF_REGEN_SFX=0` on the second pass reuses WAV stems.)
